/**
 * Схема базы данных для Kysely.
 *
 * Зачем: единая точка типов для всех таблиц; наполняется в коммите 04 вместе с DDL.
 * Как: имя ключа совпадает с именем таблицы в PostgreSQL. Пока таблиц нет — пустой Record.
 */
export type Database = Record<string, never>;
